<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cloud.pokemon_map">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="org.litepal.LitePalApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SignInActivity"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignUpActivity" />
        <activity android:name=".dbtest" />
        <activity
            android:name=".SelfInfoActivity"
            android:label="Profile"
            android:launchMode="singleTask" />
        <activity
            android:name=".PmHandbookActivity"
            android:label="Handbook"
            android:launchMode="singleTask" />
        <activity
            android:name=".PokemonItemActivity"
            android:theme="@style/PokemonItemActivityTheme"/>
    </application>

</manifest>